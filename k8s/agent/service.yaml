apiVersion: v1
kind: Service
metadata:
  name: agent-svc
  namespace: hidden-city
  labels:
    app: agent
spec:
  selector:
    app: agent
  ports:
    - name: http
      port: 80
      targetPort: 8001
  type: ClusterIP
