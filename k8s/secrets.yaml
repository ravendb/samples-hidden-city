# Template — copy to k8s/secrets.local.yaml, fill in values, apply manually.
# Never commit secrets.local.yaml (it is in .gitignore).
# Production: use Sealed Secrets, Vault, or your cloud provider's secrets manager.
apiVersion: v1
kind: Secret
metadata:
  name: hidden-city-secrets
  namespace: hidden-city
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "REPLACE_ME"
  KIWI_API_KEY: "REPLACE_ME"
  AMADEUS_CLIENT_ID: "REPLACE_ME"
  AMADEUS_CLIENT_SECRET: "REPLACE_ME"
  TRAVELPAYOUTS_TOKEN: "REPLACE_ME"
